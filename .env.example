# Global leaderboard backend (Cloudflare Worker URL from `server/`).
# Copy to `.env` and set it, or add it as an env var in Cloudflare Pages.
# Leave unset to run with the leaderboard hidden (the game works fine without it).
VITE_LEADERBOARD_URL=
